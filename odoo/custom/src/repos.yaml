# See https://github.com/Tecnativa/doodba#optodoocustomsrcreposyaml
./odoo:
  defaults:
    # Shallow repositories ($DEPTH_DEFAULT=1) are faster & thinner
    # You may need a bigger depth when merging PRs (use $DEPTH_MERGE
    # for a sane value of 100 commits)
    depth: $DEPTH_DEFAULT
  remotes:
    ocb: https://github.com/OCA/OCB.git
    odoo: https://github.com/odoo/odoo.git
    openupgrade: https://github.com/OCA/OpenUpgrade.git
  target: ocb $ODOO_VERSION
  merges:
    - ocb $ODOO_VERSION
    # Example of a merge of the PR with the number <PR>
    # - oca refs/pull/<PR>/head

hr-expense:
  defaults:
    depth: $DEPTH_MERGE
  remotes:
    origin: https://github.com/OCA/hr-expense.git
  target: origin $ODOO_VERSION
  merges:
    - origin $ODOO_VERSION
    - origin refs/pull/104/head # hr_expense_advance_clearing: not allow cancel payment
    - origin refs/pull/119/head # [14.0][ENH] hr_expense_advance_clearing, not allow over return

account-payment:
  defaults:
    depth: $DEPTH_MERGE
  remotes:
    origin: https://github.com/OCA/account-payment.git
  target: origin $ODOO_VERSION
  merges:
    - origin $ODOO_VERSION
    - origin refs/pull/490/head # [ENH] analytic on multi deduction

server-ux:
  defaults:
    depth: $DEPTH_MERGE
  remotes:
    origin: https://github.com/OCA/server-ux.git
  target: origin $ODOO_VERSION
  merges:
    - origin $ODOO_VERSION

reporting-engine:
  defaults:
    depth: $DEPTH_MERGE
  remotes:
    origin: https://github.com/OCA/reporting-engine.git
  target: origin $ODOO_VERSION
  merges:
    - origin $ODOO_VERSION

partner-contact:
  defaults:
    depth: $DEPTH_MERGE
  remotes:
    origin: https://github.com/OCA/partner-contact.git
  target: origin $ODOO_VERSION
  merges:
    - origin $ODOO_VERSION

mis-builder:
  defaults:
    depth: $DEPTH_MERGE
  remotes:
    origin: https://github.com/OCA/mis-builder.git
  target: origin $ODOO_VERSION
  merges:
    - origin $ODOO_VERSION

l10n-thailand:
  defaults:
    depth: $DEPTH_MERGE
  remotes:
    origin: https://github.com/OCA/l10n-thailand.git
  target: origin $ODOO_VERSION
  merges:
    - origin $ODOO_VERSION
    - origin refs/pull/272/head # l10n_th_account_tax, l10n_th_account_tax_expense, l10n_th_account_tax_multi, l10n_th_account_tax_report, l10n_th_account_wht_cert_form
    - origin refs/pull/286/head # [FIX] l10n_th_partner
